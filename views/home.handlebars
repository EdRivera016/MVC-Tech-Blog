{{> layouts/main}}
<section>
  <h1>Home</h1>
  {{#each posts}}
  <article>
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <small>Written by {{this.user.username}} on {{format_date this.date_created}}</small>
  </article>
  {{/each}}
</section>
